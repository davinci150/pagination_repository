import 'task_details_model.dart';
import 'task_model.dart';

/* class TaskEntity {
  TaskEntity({
    required this.id,
    required this.title,
    required this.isCompleted,
    this.description,
  });

  factory TaskEntity.fromTaskModel(TaskModel model) {
    return TaskEntity(
      id: model.id,
      title: model.title,
      isCompleted: model.isCompleted,
    );
  }

  factory TaskEntity.fromTaskDetails(TaskDetailsModel model) {
    return TaskEntity(
      id: model.id,
      title: model.title,
      isCompleted: model.isCompleted,
      description: model.description,
    );
  }

  final int id;
  final String title;
  final bool isCompleted;
  final String? description;

  TaskEntity copyWith({
    int? id,
    String? title,
    bool? isCompleted,
    String? description,
  }) {
    return TaskEntity(
      id: id ?? this.id,
      title: title ?? this.title,
      isCompleted: isCompleted ?? this.isCompleted,
      description: description ?? this.description,
    );
  }
}
 */